# 海投王施工计划

> 版本：2026-08-30 重排版
> 当前第一目标：用一份 JD 和一份 JSON 形式的个人 RAG 库，生成一份有事实依据、可人工审核的新简历。

## 1. 当前目标与成功标准

当前不再以“职位发现 → 自动填写 → 投递管理”的完整链路作为首个可用版本。第一阶段先交付一个范围更小、可以真实验证价值的简历生成 MVP：

> 输入 JD 文本 + 读取 JSON RAG 文件 → 分析岗位要求 → 召回相关个人事实 → 生成定制简历 → 校验每项陈述的事实依据 → 展示可复制的新简历与引用。

MVP 成功必须同时满足：

1. 用户可以提交一段 JD，并选择或使用一份配置好的 JSON 文件。
2. 系统能返回一份针对该 JD 调整过的新简历，而不是简单拼接事实或复述 JD。
3. 简历中的事实性陈述都能追溯到 JSON 中的记录；没有依据的内容不得编造。
4. JSON 无效、JD 为空、RAG 无相关内容、模型失败时均返回明确且可恢复的错误。
5. 生成结果默认为 review_required，只供用户审核、修改或复制，不自动批准、不自动投递。
6. 关键流程具备自动化测试，并能用固定 JD + 固定 JSON 夹具重复验收。

## 2. MVP 边界

### 2.1 本期包含

- JD：先支持纯文本输入；保留未来从 URL、PDF、截图和职位库读取的入口。
- RAG 数据源：先读取单个 JSON 文件；详细 JSON 格式待用户提供后再锁定。
- 召回：先实现可测试的关键词、标签和字段权重检索；是否调用 Embedding 不作为 MVP 成功的前提。
- 生成：通过可替换的 AI Provider 生成一份定制简历草稿。
- 可信约束：只允许使用本次召回到的证据；输出同时保留 evidence_id 引用。
- 输出：结构化简历 JSON + 可直接预览/复制的纯文本或 Markdown。
- 最小交互：一个页面完成 JD 输入、JSON 选择/状态展示、生成、结果预览和证据查看。

### 2.2 本期不作为发布阻塞项

- 简历、证书、PDF 等文件的自动解析和入库。
- PostgreSQL/pgvector 持久化、在线增量索引和多用户 RAG 管理。
- 职位自动发现、去重、匹配排行和批量生成。
- Cover Letter、开放题、多轮智能评审和自动修订。
- DOCX/PDF 精排导出和多模板系统。
- 浏览器自动填写、自动投递、申请 CRM、邮件回流、面试与分析。

这些能力仍保留在后续路线图中；已有相关代码继续保留，但不得反向扩大本 MVP 的依赖范围。

## 3. 当前代码基础与缺口

仓库已经具有 NestJS API、React Web、档案事实、内存检索、材料版本/引用/审核状态、多代理评审等基础实现。当前可复用的核心包括：

- profile：事实及来源、状态和使用范围模型。
- knowledge：切片、确定性 Embedding、混合评分和引用结果。
- materials：简历材料、事实引用、校验、版本与审核状态。
- review：后续可接入的独立评审能力。

但现有材料生成器主要按事实模板确定性拼装，尚未形成“读取 JSON → 理解 JD → 检索证据 → AI 定制简历”的用户闭环。

| 缺口 | MVP 处理方式 |
|---|---|
| 没有 JSON RAG 读取边界 | 新增 RagSource 接口和 JSON 适配器 |
| JSON 正式格式尚未确定 | 外部格式只存在于适配器；内部统一映射为证据记录 |
| JD 未进入召回和生成上下文 | 新增 JD 分析结果和编排服务 |
| 当前生成器不做针对 JD 的表达重组 | 新增可替换的 ResumeGenerator Provider |
| 缺少一条生成 API | 新增单一编排端点，不要求客户端串联多个内部服务 |
| 缺少最小用户页面 | 新增一个专注生成流程的页面 |
| 缺少该闭环的黑盒验收 | 增加固定夹具、Provider Stub 和端到端测试 |

## 4. 防止后续返工的架构边界

### 4.1 JSON 只是数据源，不是领域模型

生成流程不得直接读取任意 JSON 字段。所有外部数据先经过以下端口：

~~~text
JSON 文件
  → RagSource.load()
  → RagRecordMapper.map()
  → EvidenceRecord[]
  → Retriever.retrieve()
  → ResumeGenerationService
~~~

内部最小证据模型暂定为：

~~~ts
interface EvidenceRecord {
  id: string;
  kind: string;
  content: string;
  tags: string[];
  source: { type: string; reference: string };
  metadata: Record<string, unknown>;
}
~~~

这是内部稳定模型，不等同于用户稍后提供的 JSON 格式。收到正式格式后新增或修改 RagRecordMapper，禁止让 JSON 字段名渗透到检索、Prompt、材料校验和界面中。

在正式格式到达前，仅制作一个临时测试夹具，保证每条记录至少可以映射出 id、kind 和 content。临时格式必须标记为 provisional，不得写入对外长期契约。

### 4.2 核心能力使用可替换端口

| 端口 | MVP 实现 | 后续替换方向 |
|---|---|---|
| RagSource | 单 JSON 文件读取 | 上传文件、数据库、对象存储、用户事实库 |
| RagRecordMapper | 临时/正式 JSON 映射 | 带版本迁移的多格式映射 |
| Retriever | 关键词 + 标签 + 字段权重 | pgvector 混合检索、重排模型 |
| JdAnalyzer | AI Provider 或可测试 Stub | 职位解析器、结构化职位库、规则引擎 |
| ResumeGenerator | 单模型 Provider | 多模型路由、模板策略、多语言 |
| ClaimVerifier | evidence ID + 关键字段校验 | NLI、评审代理、人工确认闭环 |
| ResumeRenderer | JSON + 文本/Markdown | DOCX、PDF、模板中心 |
| GenerationRepository | 可先不持久化或复用材料存储 | PostgreSQL 版本、差异、审计和回滚 |

业务服务只能依赖这些端口，不直接依赖具体 JSON 格式、模型 SDK、向量数据库或导出库。

### 4.3 单次生成输入与输出契约

建议新增面向用户流程的端点：

~~~text
POST /v1/resume-generations
~~~

请求的稳定语义：

- jd_text：必填的 JD 原文。
- rag_source：MVP 为上传的 JSON 或服务端配置的测试文件引用；禁止读取任意服务器路径。
- preferences：可选的语言、篇幅、目标职级等约束。
- idempotency_key：防止重复请求产生多份结果。

响应至少包含：

- generation_id、status、生成与 Prompt 版本。
- 结构化 resume 及可预览文本。
- jd_analysis：岗位、必备条件、加分项和关键词。
- retrieved_evidence：本次使用和未使用的证据及分数。
- 每条简历 claim 的 evidence_ids。
- warnings：事实缺口、低相关性、待确认和降级信息。

API 只暴露业务语义，不暴露模型供应商、向量实现或临时 JSON 字段。

## 5. MVP 施工任务图

任务按依赖顺序执行。每个任务只修改自己的范围；共享类型的变更先完成契约再实现。

### R0：契约与固定验收样本

#### resume_mvp_contract — 锁定内部模型与 API 契约

- 依赖：无。
- 写入：contracts、docs/architecture。
- 内容：定义 EvidenceRecord、JD 分析、检索结果、简历 section/claim、引用、warning、生成状态、错误码和幂等语义。
- 约束：JSON 外部字段仍标为待定；只锁定 Mapper 之后的内部模型。
- 验收：契约可机器校验；含成功、JSON 无效、无相关事实、模型失败和事实校验失败样例。

#### resume_mvp_fixtures — 建立临时 JSON 与 JD 黄金样本

- 依赖：resume_mvp_contract。
- 写入：services/api/tests/resume-generation/fixtures、tests/e2e/resume-mvp/fixtures。
- 内容：至少包含一个正常 JSON、一个缺字段 JSON、一个非法 JSON；JD 覆盖高匹配、部分匹配和无匹配。
- 验收：夹具不含真实个人敏感信息；明确标记临时 JSON 格式，待正式格式到达后只替换 Mapper 与夹具。

### R1：JSON RAG 数据源

#### json_rag_adapter — JSON 读取、校验与映射

- 依赖：resume_mvp_contract、resume_mvp_fixtures。
- 写入：services/api/src/modules/resume-generation/rag 及对应测试。
- 内容：实现 RagSource、RagRecordMapper、文件大小/编码/JSON 语法检查、重复 ID 检查和内部证据映射。
- 安全：仅接受上传流或配置允许目录中的文件引用；不记录完整简历内容；限制文件大小、记录数和单条长度。
- 验收：有效 JSON 稳定映射；非法、空、超限、重复 ID 和无法映射的记录返回可读错误；原始字段不泄漏到后续模块。

#### json_schema_followup — 接入用户提供的正式 JSON 格式

- 依赖：用户提供格式、json_rag_adapter。
- 写入：只修改 JSON Schema、Mapper、迁移兼容逻辑与夹具。
- 内容：确定 schema_version、必填/可选字段、枚举、日期、来源、敏感字段和版本兼容策略。
- 验收：正式格式至少有一个真实脱敏样本通过；旧临时夹具可删除或由迁移器明确拒绝；检索与生成模块无需修改。
- 状态：等待用户提供格式，不阻塞其余模块用临时夹具开发。

### R2：JD 分析与证据召回

#### jd_analysis — 将 JD 转成稳定的岗位需求模型

- 依赖：resume_mvp_contract。
- 写入：services/api/src/modules/resume-generation/jd 及对应测试。
- 内容：提取岗位名称、职责、必备条件、加分项、技术/业务关键词、职级和语言；保留原文片段依据。
- 验收：固定 Provider Stub 下输出确定；Prompt 注入文本只被当作 JD 内容；空白或超长 JD 被拒绝。

#### evidence_retrieval — 用 JD 召回个人证据

- 依赖：json_rag_adapter、jd_analysis。
- 写入：services/api/src/modules/resume-generation/retrieval，必要时复用 knowledge，以及对应测试。
- 内容：按 JD 关键词、记录类型、标签和必备项覆盖度评分；去重并限制上下文大小；返回分数和命中原因。
- 验收：黄金样本中预期经历进入 Top-K；无关记录不会因关键词堆砌获得最高分；同输入结果稳定；空召回进入“事实不足”而非编造路径。

### R3：可信简历生成

#### ai_provider_adapter — 可替换的模型调用层

- 依赖：resume_mvp_contract。
- 写入：services/api/src/modules/ai 或既有统一 Provider 目录，以及对应测试。
- 内容：统一模型请求、结构化输出、超时、重试、限流、模型/Prompt 版本和 token/cost 元数据；测试使用本地 Stub，不依赖真实 API。
- 验收：业务代码中没有供应商 SDK 类型；模型超时和无效结构可降级；密钥不进入仓库、响应或日志。

#### resume_generation_orchestrator — 串起完整生成流程

- 依赖：evidence_retrieval、ai_provider_adapter。
- 写入：services/api/src/modules/resume-generation、必要的 materials 适配代码及对应测试。
- 流程：加载 JSON → 分析 JD → 检索 → 构造只含允许证据的 Prompt → 生成结构化简历 → claim 引用校验 → 渲染预览 → 保存或返回。
- 生成原则：优化排序、措辞和重点，不更改公司、学校、职位、时间、技能熟练度、数字成果等事实；JD 中出现但证据中不存在的技能不得写成“会”。
- 验收：每条事实 claim 至少有一个有效 evidence_id；引用不存在、结构错误、残留占位符或未经支持的新事实会阻止结果进入 review_required；同一幂等键不重复生成。

#### claim_verification — 独立事实门禁

- 依赖：resume_generation_orchestrator。
- 写入：services/api/src/modules/resume-generation/verification 及黄金测试。
- 内容：检查引用存在性、关键实体/日期/数字保真、待确认内容和缺失项；验证逻辑不得仅依赖生成模型的自我声明。
- 验收：预置的公司、日期、技能和量化结果篡改样本 100% 被阻止；证据不足时给 warning 或失败，不自动补写。

### R4：API 与最小页面

#### resume_generation_api — 单端点生成能力

- 依赖：claim_verification。
- 写入：services/api/src/modules/resume-generation、应用模块挂载、契约测试。
- 内容：实现创建生成请求、输入校验、统一错误映射和结果返回；耗时过长时可在保持响应契约不变的情况下切换为队列任务。
- 验收：通过 HTTP 完成完整闭环；认证用户之间数据隔离；错误码与契约一致；任意服务器文件路径不可读取。

#### resume_generation_web — 单页生成工作台

- 依赖：resume_generation_api、既有 Web 骨架。
- 写入：apps/web/src/features/resume-generation 及页面挂载和测试。
- 内容：JD 输入、JSON 文件选择或已配置状态、生成按钮、加载/错误/空结果、简历预览、证据展开和复制。
- 验收：用户无需理解 RAG 或内部模块即可完成生成；生成中防重复提交；warning 和“待人工审核”明显展示；键盘可操作。

### R5：MVP 验收与交付

#### resume_mvp_e2e — 黑盒验收

- 依赖：resume_generation_api、resume_generation_web。
- 写入：tests/e2e/resume-mvp、docs/qa/resume-mvp-report.md。
- 内容：以固定 JD 和 JSON 从页面/API 生成简历，校验结果、引用、错误路径、租户隔离和幂等。
- 退出条件：
  - 正常样本一次操作得到可读的新简历；
  - 所有事实 claim 引用有效；
  - 无相关事实时不编造；
  - 非法或超限 JSON 和模型故障可恢复；
  - lint、typecheck、unit、contract、E2E 全部通过；
  - 验收报告记录实际命令、环境、模型、Prompt、夹具版本和遗留风险。

#### resume_mvp_release — 可运行交付

- 依赖：resume_mvp_e2e。
- 写入：README、示例配置、最小运行手册。
- 内容：说明如何配置模型、放置或上传 JSON、启动应用、运行验收和排查常见错误。
- 验收：在干净环境可按文档完成一次生成；示例配置不含密钥或真实个人数据。

## 6. 依赖图与实施顺序

~~~text
resume_mvp_contract
  ├─ resume_mvp_fixtures → json_rag_adapter ─┐
  ├─ jd_analysis ─────────────────────────────┼─ evidence_retrieval ─┐
  └─ ai_provider_adapter ────────────────────────────────────────────┤
                                                                      ↓
                                             resume_generation_orchestrator
                                                                      ↓
                                                    claim_verification
                                                                      ↓
                                                    resume_generation_api
                                                                      ↓
                                                    resume_generation_web
                                                                      ↓
                                                       resume_mvp_e2e
                                                                      ↓
                                                     resume_mvp_release

用户提供正式 JSON 格式 → json_schema_followup
                           （只替换 Schema、Mapper、夹具，可在 R1 后插入）
~~~

首条可演示纵切建议按以下顺序完成：

1. 用临时 JSON 夹具跑通 JSON → EvidenceRecord[]。
2. 用固定 JD 和本地 Stub 跑通分析 → 召回 → 结构化简历。
3. 加入事实引用校验，确保编造样本无法通过。
4. 暴露单一 API，再接最小页面。
5. 最后接真实模型和正式 JSON Mapper，避免外部依赖阻塞主链路开发。

## 7. 后续路线图

当前 MVP 完成后，后续功能按下列顺序扩展。每一阶段都复用第 4 节端口，不回头重写生成核心。

| 阶段 | 目标 | 复用或替换点 | 退出条件 |
|---|---|---|---|
| P1 RAG 正式化 | 正式 JSON Schema、版本迁移、用户上传和数据管理 | 替换 RagRecordMapper，扩展 RagSource | 正式脱敏样本、错误迁移和兼容策略通过 |
| P2 检索持久化 | PostgreSQL/pgvector、增量索引、删除同步、重排 | 替换 Retriever | 隔离和删除正确，黄金集 Recall@10 达标 |
| P3 生成增强 | 基础简历选择、多语言、模板、DOCX/PDF、版本差异 | 扩展 ResumeRenderer 和材料仓库 | 导出可解析，版本和引用可回溯 |
| P4 智能评审 | ATS、招聘官、事实、自然度独立评审与有限修订 | 接入既有 review | 已知编造和矛盾全部进入必须修复或人工审核 |
| P5 职位闭环 | 职位导入、去重、风险、匹配评分和批量生成 | JD 来源切换为职位库 | 从职位导入到批准材料的 E2E 通过 |
| P6 辅助投递 | 本地代理、表单识别、上传、提交前确认和 CRM | 消费已批准材料 | 默认不自动提交；异常安全降级且无重复提交 |
| P7 生命周期 | 日报、邮件回流、测评、面试、分析 | 消费申请事件 | 幂等关联、合法状态迁移、指标口径可追溯 |
| P8 规则内自动化 | 白名单、限额、急停下的等级 2 投递 | 扩展策略端口 | 仅在授权与全部硬门满足时提交 |

## 8. 质量、安全与重规划触发器

### 8.1 始终生效的门禁

- 事实优先：检索分数和模型判断都不能覆盖事实引用门禁。
- 提示注入隔离：JD 和 JSON 内容均视为不可信数据，不得改变系统规则、读取密钥或扩大工具权限。
- 最小数据暴露：只向模型发送生成所需的 JD 片段和 Top-K 证据；日志不记录完整个人资料或模型密钥。
- 可复现：保存模型、Prompt、Schema、Mapper、检索策略和夹具版本。
- 可替换：功能代码不得直接创建模型 SDK 客户端、读取任意文件路径或拼接数据库查询。
- 人工终审：MVP 结果永远处于待审核状态，不因评分高而自动批准或投递。

### 8.2 必须重规划的情况

- 正式 JSON 格式无法无损映射为当前 EvidenceRecord 时，先版本化扩展内部契约，再改 Mapper；不得在下游散加例外字段。
- 模型无法稳定输出结构化结果时，先加强 Provider 修复或重试，或改用两阶段生成，不让界面解析自然语言。
- 黄金样本发现事实校验存在漏检时，暂停真实用户发布，先补失败夹具和独立规则。
- 单次生成超过已确认的时延或成本上限时，把 API 实现切换为异步队列；请求和结果业务契约保持不变。
- RAG 规模超过内存方案承载范围时替换 Retriever，不改变生成编排和 API。

## 9. 工单规则

- 每张工单只领取一个任务 key，继承该任务的依赖、写入范围和验收条件。
- 只有依赖全部完成且有效时才能开工；不得用未确认的正式 JSON 字段阻塞临时夹具纵切。
- 契约、实现和验收夹具应分别提交，确保格式变化可以独立审查和回滚。
- 工单回执必须记录实际测试命令、环境和结果；模型相关工单还需记录模型和 Prompt 版本。
- 发现范围外问题时登记风险或新工单，不顺手把职位、投递或分析能力塞进简历 MVP。
- 后续功能优先新增适配器或扩展稳定端口；只有领域语义确实变化时才修改核心契约。
