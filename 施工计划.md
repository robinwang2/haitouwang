# 海投王施工计划

## 1. 计划目标与边界

本计划依据《海投王_智能求职平台完整产品策划案_更新版》制定。首版交付目标是完成以下 MVP 闭环：

> 建立求职档案与事实库 → 发现/导入职位 → 标准化、去重、风险与匹配判断 → 基于可验证事实生成材料 → 多代理评审与用户审核 → 本地浏览器辅助填写 → 保存申请记录与日报。

MVP 默认停在“提交前用户确认”的自动化等级 1。验证码、在线测评、视频、电子签名、未知法律/身份问题、歧义工作授权，以及无法确认提交结果的页面必须降级为人工处理。全平台无人值守投递、绕过平台限制、自动编造经历均不在 MVP 范围内。

本仓库目前只有产品策划案，没有既有代码、接口、工程约定或已锁定的技术选型。因此 M0 必须先完成产品决策、安全边界与接口契约；后续工单不得把策划案中的“推荐技术栈”当作已确认结论。

## 2. 里程碑

| Milestone | 目标 | 退出条件 |
|---|---|---|
| M0 决策与工程基线 | 锁定 MVP 产品边界、信任边界、领域模型、接口和工程骨架 | 决策记录获产品负责人确认；契约可机器校验；威胁模型完成；空骨架 CI 通过 |
| M1 职位发现与匹配 | 建立档案/事实库，导入 Greenhouse、Lever、公司 Careers 与手动链接，完成标准化、去重、风险和评分 | 端到端测试能从来源夹具得到唯一职位记录、硬规则结果、分数与解释，并在 Web 查看 |
| M2 材料生成与智能评审 | 完成混合检索、简历/Cover Letter/开放题生成、事实溯源、差异和独立多代理评审 | 未授权事实不能进入材料；已知缺陷会被拦截；用户可查看依据、修改并批准 |
| M3 本地辅助申请（MVP） | 安全配对云端与本地代理，完成表单识别、材料上传、提交前确认、异常降级、申请 CRM 与日报 | Greenhouse/Lever 夹具流程可复现；默认不自动提交；申请证据和状态可追踪；MVP 验收与发布门禁通过 |
| M4 规则内自动投递 | 在白名单、硬规则、限额和总开关保护下开放自动化等级 2 | 任何未知问题或异常都会暂停；重复/越权/超限提交被测试拦截 |
| M5 求职生命周期 | 邮件回流、测评任务、面试中心与准备包 | 邮件可幂等关联申请并驱动合法状态迁移；面试任务和时区经测试 |
| M6 分析与优化 | 材料、来源、方向和漏斗分析及数据导出 | 指标口径可追溯；小样本不宣称因果；全量回归、安全与恢复演练通过 |

里程碑表示依赖顺序，不代表日历承诺；排期需在团队容量和产品决策完成后另行估算。

## 3. 跨模块契约与所有权

- `contracts/**` 和 `docs/architecture/**` 仅由 `domain_contracts` 工单写入。功能工单发现契约问题时提交变更请求，不直接并行修改。
- 根级工程配置、工作区依赖、CI 模板和开发环境仅由 `platform_scaffold` 写入；发布配置和运行手册仅由 `mvp_release` 写入。
- 服务按领域模块隔离；数据库迁移使用领域命名空间，禁止不同工单修改同一迁移文件。
- Web 功能按 `apps/web/src/features/<feature>/**` 隔离；本地代理独占 `apps/agent/**`。
- 跨模块验收测试写入独立的 `tests/e2e/<milestone>/**`，不通过 E2E 工单顺手修功能实现。
- 接口基线必须包含：OpenAPI、事件 JSON Schema、错误码、幂等键、分页、版本策略、状态机、审计事件、文件元数据、云端/本地代理配对协议。

建议的逻辑边界（技术实现待 `decision_gate` 确认）：Web 客户端、云端 API/任务执行器、PostgreSQL/向量检索、Redis/队列、本地 Playwright 代理、对象存储、AI Provider Adapter。原始登录凭证和 Cookie 不进入云端、模型输入或普通日志。

## 4. 任务图

### M0：决策与工程基线

#### `decision_gate` — MVP 产品与技术决策门禁（product）

- 依赖：无
- 写入：`docs/decisions/product-baseline.md`
- 范围：由产品负责人确认首发地区/语言、Greenhouse/Lever/Careers/手动链接的具体边界、自动化默认等级、Web/后端/本地端/队列/向量库选型、模型与数据驻留、保存/删除周期、部署与商业版本边界。
- 验收：上述决策逐项记录决策人和日期；MVP 项不得存在未分配的 TBD；未决定项明确标为阻塞后续任务。

#### `security_model` — 隐私与威胁模型（security）

- 依赖：`decision_gate`
- 写入：`docs/security/**`
- 范围：数据分类、云端/本地/第三方信任边界、凭证隔离、加密、最小权限、日志脱敏、删除/撤权、模型数据策略、滥用与误投防护。
- 验收：覆盖登录、文件、事实库、AI、浏览器会话和邮件授权；列出威胁、控制、剩余风险和责任人；明确原始凭证/Cookie/验证码不得进入模型与日志。

#### `domain_contracts` — 领域与接口契约（integrator）

- 依赖：`decision_gate`、`security_model`
- 写入：`contracts/**`、`docs/architecture/**`
- 范围：用户/目标、事实、材料、职位、评分、评审、代理、申请、任务、通知、面试、分析的领域模型；OpenAPI、事件、状态机和云地配对协议。
- 验收：OpenAPI 和 JSON Schema 可由标准工具校验；给出字段约束、错误码、幂等、分页、版本、审计与合法状态迁移；提供成功、拒绝和人工降级样例。

#### `ux_spec` — 核心流程与交互规格（design）

- 依赖：`decision_gate`
- 写入：`docs/ux/**`
- 范围：首页、职位池、审核中心、申请看板、资料中心、平台连接、规则中心、面试和分析页面；桌面代理配对与人工接管流程。
- 验收：每条 MVP 用户旅程有页面流、字段和操作；覆盖 loading/empty/error/permission/paused 状态、响应式和无障碍要求；高风险确认不可被普通操作误触。

#### `platform_scaffold` — 工程骨架与质量门禁（integrator）

- 依赖：`domain_contracts`、`security_model`
- 写入：根级工程配置、`.github/**`、`tooling/**`、`infra/dev/**`、各应用入口骨架
- 范围：按已确认技术栈建立工作区、配置管理、测试分层、本地依赖、CI、格式化、静态检查、依赖与密钥扫描；仅创建功能模块挂载点。
- 验收：干净环境按 README 可一键启动依赖并执行 lint/typecheck/unit/contract；CI 全绿；示例配置无秘密；功能模块无需改根配置即可接入。

### M1：职位发现与匹配

#### `profile_facts` — 求职档案、事实库与简历库（backend）

- 依赖：`platform_scaffold`、`domain_contracts`
- 写入：`services/api/src/modules/profile/**`、`services/api/tests/profile/**`、该领域迁移
- 范围：多求职目标、基础资料、事实状态/范围、工作授权、简历与文件元数据、版本、确认、撤销和删除。
- 验收：CRUD、版本和审计测试通过；待确认/过期/禁止/范围限制事实不能被当作可用事实；用户可导出和删除自己的数据；敏感字段不出现在日志。

#### `job_pipeline` — 职位导入、标准化、去重与风险（backend）

- 依赖：`platform_scaffold`、`domain_contracts`
- 写入：`services/api/src/modules/jobs/**`、`services/api/tests/jobs/**`、该领域迁移
- 范围：Greenhouse、Lever、公司 Careers 页面和手动 URL；规范字段、来源追踪、跨源去重、过期检测、真实性风险与连接器降级。
- 验收：固定夹具可重复解析；同职位跨源只产生一条主记录并优先官网；连接器失败不污染已有数据；诈骗/过期/缺字段样本输出解释性风险和人工处理状态。

#### `matching_engine` — 硬规则、匹配评分与解释（backend）

- 依赖：`profile_facts`、`job_pipeline`
- 写入：`services/api/src/modules/matching/**`、`services/api/tests/matching/**`
- 范围：先执行工作授权、黑名单、重复、地点/薪资/类型与风险门控，再按策划案 25/20/15/15/10/10/5 权重评分，输出优势、不足和建议。
- 验收：权重合计 100；硬规则优先于分数且禁止被模型覆盖；同输入结果确定；黄金样本覆盖各分段和全部硬门；解释能引用输入字段。

#### `core_web` — 档案、职位池与控制台（frontend）

- 依赖：`platform_scaffold`、`domain_contracts`、`ux_spec`
- 写入：`apps/web/src/features/profile/**`、`apps/web/src/features/jobs/**`、`apps/web/src/features/dashboard/**`
- 范围：入门流程、事实确认、简历库、职位筛选/详情、匹配解释、平台状态和自动化总开关展示。
- 验收：由契约生成/校验类型；关键页面覆盖 loading/empty/error/permission；键盘可用且高风险状态有文本而非仅颜色；组件与契约 mock 测试通过。

#### `m1_e2e` — M1 集成验收（qa）

- 依赖：`profile_facts`、`job_pipeline`、`matching_engine`、`core_web`
- 写入：`tests/e2e/m1/**`、`docs/qa/m1-report.md`
- 范围：从建档、导入到职位池和评分的黑盒验收。
- 验收：手动、Greenhouse、Lever 和 Careers 夹具均通过；跨源重复被合并；不符合工作授权和高风险职位被拦截；报告记录命令、环境与可复现证据。

### M2：材料生成与智能评审

#### `rag_retrieval` — 事实索引、混合检索与重排序（ai/backend）

- 依赖：`profile_facts`、`job_pipeline`、`matching_engine`
- 写入：`services/api/src/modules/knowledge/**`、`services/api/tests/knowledge/**`
- 范围：解析、切片、Embedding、关键词/向量/标签混合检索、按身份/状态/范围/时间过滤、重排、出处和删除同步。
- 验收：每条结果含事实 ID、版本和来源；禁止/过期/越范围事实召回率为 0；删除后索引同步；批准的黄金集 Recall@10 不低于 0.80，并固化评测报告。

#### `material_generation` — 简历、Cover Letter 与开放题生成（ai/backend）

- 依赖：`rag_retrieval`、`matching_engine`
- 写入：`services/api/src/modules/materials/**`、`services/api/tests/materials/**`
- 范围：基础简历选择、材料生成、事实引用、字数/ATS/占位符检查、版本差异、DOCX/PDF/文本输出和成本策略。
- 验收：每项陈述可追溯到允许事实或标为待确认且不得发布；固定测试禁止改写学校/GPA/公司/时间和新增技能；输出可解析、无残留占位符；版本与差异可重现。

#### `application_review` — 独立多代理申请评审（ai/backend）

- 依赖：`material_generation`
- 写入：`services/api/src/modules/review/**`、`services/api/tests/review/**`、`evals/review/**`
- 范围：ATS、硬条件、匹配、怀疑型招聘官、Hiring Manager、RAG 事实与自然度独立评审；汇总、有限自动修订、事实复核和第二轮盲审。
- 验收：生成器与评审器配置隔离；并行评审保留各自证据；循环次数有上限；已知矛盾/编造/关键缺字段样本 100% 进入必须修复或人工审核；最终建议不由单一总分决定。

#### `review_ui` — 审核中心（frontend）

- 依赖：`domain_contracts`、`ux_spec`、`application_review`
- 写入：`apps/web/src/features/review/**`
- 范围：职位、材料差异、事实证据、各评审结果、必须修复项、编辑、批准/拒绝和人工问题收集。
- 验收：用户能追到每项材料的事实来源；必须修复项未关闭时不能批准；批准/编辑/拒绝均生成审计事件；长材料和失败状态有组件测试。

#### `m2_e2e` — M2 集成与 AI 评测（qa）

- 依赖：`m1_e2e`、`application_review`、`review_ui`
- 写入：`tests/e2e/m2/**`、`docs/qa/m2-report.md`
- 范围：真实事实到批准材料的闭环、提示注入/越权事实/模型失败降级和成本基线。
- 验收：黄金用例输出可追溯材料；越权与提示注入样本不泄露事实；模型超时/限流降级为可恢复状态；评测记录模型版本、提示版本、数据集版本和结果。

### M3：本地辅助申请与 MVP 发布

#### `agent_pairing` — 云端与本地代理安全配对（backend/security）

- 依赖：`platform_scaffold`、`domain_contracts`、`security_model`
- 写入：`services/api/src/modules/agent/**`、`services/api/tests/agent/**`
- 范围：一次性配对、短期范围令牌、任务领取/回执、心跳、撤权、重放保护和脱敏审计。
- 验收：过期/撤销/越范围/重复令牌均被拒绝；云端不接收原始密码、Cookie 或验证码；协议支持幂等重试；安全测试覆盖窃取和重放场景。

#### `local_apply_agent` — 本地浏览器辅助填写（desktop/automation）

- 依赖：`agent_pairing`、`application_review`
- 写入：`apps/agent/**`
- 范围：本地会话、Greenhouse/Lever 表单识别、字段映射、文件上传、预览、提交前暂停、截图/确认编号、异常检测和人工接管。
- 验收：在固定浏览器夹具上可完成填写与上传；MVP 默认永不越过提交确认；验证码、未知必填题、法律/签名/工作授权歧义、页面变化和无法确认结果时停止且不重复提交；日志无敏感信息。

#### `automation_ui` — 平台连接、规则与本地接管界面（frontend）

- 依赖：`agent_pairing`、`ux_spec`、`domain_contracts`
- 写入：`apps/web/src/features/connections/**`、`apps/web/src/features/automation/**`
- 范围：代理配对、连接状态、独立搜索/生成/填写/提交开关、限额、暂停原因、任务进度和人工接管。
- 验收：默认自动化等级 1 且提交关闭；所有开关变更需明确确认并写审计；掉线/撤权/暂停/过期状态可恢复；未知状态不会显示为成功。

#### `application_crm` — 申请记录、看板与手动任务（fullstack）

- 依赖：`job_pipeline`、`application_review`、`local_apply_agent`、`ux_spec`
- 写入：`services/api/src/modules/applications/**`、该领域迁移、`apps/web/src/features/applications/**`
- 范围：策划案状态机、材料/截图/确认编号关联、完整时间线、手动申请任务包、截止时间和看板。
- 验收：仅允许契约定义的状态迁移；重复回执幂等；提交成功必须有可验证证据，否则保持待确认；手动任务包含链接、材料、答案、风险和未决项；看板筛选和无障碍测试通过。

#### `daily_reporting` — 通知与 MVP 日报（backend）

- 依赖：`application_crm`、`job_pipeline`
- 写入：`services/api/src/modules/reporting/**`、`services/api/tests/reporting/**`
- 范围：高匹配/待审核/登录过期/失败/逾期通知，以及发现、去重、投递、待确认、手动任务、过滤和异常日报。
- 验收：同一事件不会重复通知；用户时区与退订/静默规则生效；日报数字可回查源记录；敏感材料正文不进入通知载荷。

#### `mvp_e2e` — MVP 端到端、安全与恢复验收（qa）

- 依赖：`m2_e2e`、`local_apply_agent`、`automation_ui`、`application_crm`、`daily_reporting`
- 写入：`tests/e2e/mvp/**`、`docs/qa/mvp-report.md`
- 范围：完整 MVP 主路径、人工降级、并发/幂等、权限、删除、备份恢复、性能和浏览器版本矩阵。
- 验收：全部 MVP 验收用例通过；零高危安全问题和零已知重复提交；恢复演练达到 `decision_gate` 确认的 RPO/RTO；关键性能达到已确认 SLO；失败证据和遗留风险已登记。

#### `mvp_release` — 发布集成与运行手册（integrator/devops）

- 依赖：`mvp_e2e`
- 写入：`infra/release/**`、`docs/runbooks/**`、发布清单
- 范围：部署、迁移、回滚、监控告警、功能开关、供应商故障降级、隐私请求、事故响应和版本发布门禁。
- 验收：预发布环境按清单从零部署；迁移与回滚演练通过；告警覆盖任务积压、模型/连接器/代理失败和异常提交；发布清单由产品、安全、QA 和运维签字。

### M4-M6：MVP 后能力

#### `controlled_auto_apply` — 白名单规则内自动投递（automation/security）

- 依赖：`mvp_release`
- 写入：`services/api/src/modules/policy/**`、`apps/agent/src/features/auto_apply/**`、`tests/e2e/m4/**`
- 范围：自动化等级 2、白名单平台、高匹配/无未知问题门控、公司/类别/时段限额、全局急停、异常暂停和提交证据。
- 验收：仅同时满足白名单、硬规则、材料批准、答案已知、限额和用户授权时提交；急停即时阻止新任务；验证码/警告/结构变化自动暂停；重复、越权和超限用例全部被拦截。

#### `email_lifecycle` — 邮件回流与测评任务（backend）

- 依赖：`mvp_release`、`application_crm`、`security_model`
- 写入：`services/api/src/modules/email/**`、`services/api/tests/email/**`
- 范围：最小权限邮箱授权、确认/联系/测评/面试/拒绝/Offer 分类、申请关联、状态更新和测评截止任务。
- 验收：固定邮件集分类与关联达到 `decision_gate` 确认阈值；重复 webhook 幂等；低置信度不自动改状态；撤权后停止同步并按策略删除；邮件正文不进入普通日志。

#### `interview_center` — 面试中心与准备包（fullstack）

- 依赖：`email_lifecycle`、`rag_retrieval`、`ux_spec`
- 写入：`services/api/src/modules/interviews/**`、`apps/web/src/features/interviews/**`、该领域测试与迁移
- 范围：面试日历、时区、轮次、会议链接、准备包、STAR 故事、反问、面后记录与受控回写 RAG。
- 验收：跨时区/夏令时测试通过；邮件生成的事件可确认、修改和去重；准备包仅引用允许事实；面后内容需用户确认才进入可复用知识。

#### `analytics` — 漏斗、材料与来源分析（data/fullstack）

- 依赖：`controlled_auto_apply`、`interview_center`
- 写入：`services/api/src/modules/analytics/**`、`apps/web/src/features/analytics/**`、`analytics/**`
- 范围：投递/回复/面试/Offer 漏斗、响应和耗时、材料/来源/方向表现、周报、成本指标以及 CSV/JSON/Markdown 导出。
- 验收：每项指标有版本化口径和源字段；时区、去重和状态回退测试通过；小样本明确展示样本量且不输出因果结论；导出与页面数值一致。

#### `final_qa` — 全量回归与规模化就绪评审（qa/security）

- 依赖：`analytics`
- 写入：`tests/e2e/final/**`、`docs/qa/final-report.md`
- 范围：全链路回归、连接器契约、浏览器矩阵、AI 评测、性能、可观测性、隐私删除、备份恢复和灾难/供应商故障演练。
- 验收：所有发布门禁通过；高危问题为 0；AI/连接器/浏览器基线可重复；恢复与急停演练有证据；剩余风险有责任人、截止时间和明确发布决定。

## 5. 关键风险与重规划触发器

- **未决选型**：后端、桌面端、向量库和 AI 数据策略若未确认，`domain_contracts` 之后的实现任务不得启动。
- **平台合规与页面变化**：连接器必须模块化、夹具化并可独立熔断；平台条款发生变化时暂停对应连接器，不扩大绕过能力。
- **隐私与凭证**：若无法证明云端不接触原始登录凭证，应停止本地代理相关发布。
- **AI 编造与误投**：事实审计、多代理评审、用户确认和硬规则是独立门禁，任何一层不得以总分替代。
- **契约漂移**：功能需要修改共享契约时新增 Integrator 变更工单，并让受影响任务依赖它，禁止并行抢改。
- **评测不足**：职位、事实、材料和邮件黄金集未建立前，不以人工抽查代替客观验收。

## 6. 工单分配规则

每张后续工单只领取一个 `key`，继承其依赖、写入范围与验收标准。只有 `dependsOn` 全部完成且仍有效时才能开工。工单回执必须记录实际测试命令、环境和结果；范围外缺陷进入风险或后续工单，不顺手扩张。若产品决策改变，保留已完成且兼容的契约、测试夹具和模块，重新规划受影响节点，不推倒无关产出。
